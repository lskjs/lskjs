import styled from '@emotion/styled'

export default styled('section')`
  margin-bottom: 4rem;
  font-size: 2rem;
  text-align: center;
  font-weight: 400;
  color: #000;
  font-family: ${p => p.theme.fontFamily};
`;
